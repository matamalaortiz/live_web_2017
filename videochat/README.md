# electron videoChat

## Create peerjs Key

• Create a ./sources/keys.js file and include:

```ts
var keys = {}

module.exports = keys

keys.app = {
  peer: 'your-api-key'
}
```

```bash
npm install

npm start

```
